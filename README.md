Implementação simples de um WebCrawler utilizando o módulo nodejs Puppeteer, que permite manipular Headless Chrome.

## Executando a aplicação

* Instale as dependências:
```
npm i
```
* Execute a aplicação com o node:
```
node index.js
```